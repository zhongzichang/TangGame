using System;
using UnityEngine;
using TangNet;

namespace TangGame.Net
{
/// 追加仓库的格子
public class AppendStorageRequest : Request
{
    public NetData NetData
    {
        get
        {
            return new NetData(StorageProxy.S_APPEND_STORGE);
        }
    }
}
}
