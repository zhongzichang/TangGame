using System;
using UnityEngine;
using PureMVC.Patterns;
using TangNet;
namespace TangGame.Net
{
public class ExitGameRequest : Request
{
    public NetData NetData
    {
        get
        {
            return new NetData(HeroProxy.EXIT_GAME);
        }
    }
}
}
