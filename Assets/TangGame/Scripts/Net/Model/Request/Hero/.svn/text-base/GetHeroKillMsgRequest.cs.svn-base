using System;
using UnityEngine;
using PureMVC.Patterns;
using TangNet;

namespace TangGame.Net
{
public class GetHeroKillMsgRequest : Request
{
    public NetData NetData
    {
        get
        {
            return new NetData(HeroProxy.GET_HERO_KILL_MSG);
        }
    }
}
}
