using UnityEngine;

namespace TangScene
{
  
  [RequireComponent( typeof(Locational) )]
  public class StageBhvr : MonoBehaviour {

    public int x;
    public int y;
    public int width;
    public int height;
    public string materialName;

    /*
    public const float CLICK_EXPIRE = 0.3F; // second
    private bool mouseDownAvailable = false;
    private float mouseDownRemain = 0F;
    
    */


    /*

    void Update()
    {
      if(mouseDownAvailable && mouseDownRemain < CLICK_EXPIRE)
	{
	  mouseDownRemain += Time.deltaTime;
	}
      else
	{
	  mouseDownAvailable = false;
	  mouseDownRemain = 0F;
	}
    } // m.Update

    void OnMouseDown()
    {
      mouseDownAvailable = true;
    } // m.OnMouseDown
    */
    /*
    void OnMouseUp()
    {

//      mouseDownAvailable = false;
//      if( mouseDownRemain < CLICK_EXPIRE )
//	{
      
	  Ray ray = Camera.main.ScreenPointToRay( Input.mousePosition );
	  RaycastHit hit;
	  if( Physics.Raycast( ray, out hit ))
	    {

	      if( CommonDelegates.sceneClickHandler != null )
		{
		  CommonDelegates.sceneClickHandler( hit.point );
		}


	    }
//	}
    } // m.OnMouseUp
    */

  }
}

